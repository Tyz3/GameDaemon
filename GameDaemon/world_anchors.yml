'1':
  # Якорь размечается с помощью координат центра {center}
  # и работает на условии проверки мобов в 9 чанках, где
  # {center} центральный.
  # 
  # Если мобов в области чанков 3х3 не будет, то волна 
  # засчитывается как выполненная, и якорь готовится к следующей.
  # 
  # {startIn}: Запланированное время на каждый якорь может совпадать,
  # в таком случае запустятся сразу несколько якорей независимо.
  # 
  # {center}: Центральная точка якоря, она же центральный чанк.
  # 
  # {rewards}: Награда, которая выдаётся после успешного завершения волны,
  # причём если игрок не был на её начале награда не выдаётся.
  # 
  # Минимальное кол-во игроков для старта призыва мобов: 3.
  # Радиус проверки игроков от {center}: 24
  # Время ожидания игроков до начала волны: 15 минут.
  # 
  title: 'Title1'
  startIn: '22:02,20:00'
  center: 'world -150 60 -150'
  phases:
    '1':
      hasBoss: false
      maxDuration: 600
      commands:
      - ''
    '2':
      hasBoss: true
      maxDuration: 600
      commands:
      - ''
      rewards:
      - 'IRON_HELMET;0;&bЖелезный шлем;null;null;1'
      - 'IRON_HELMET;0;&bЖелезный 22;null;null;1'
